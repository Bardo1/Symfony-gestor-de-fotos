{% extends 'walteralumnosBundle::main.html.twig' %}



{% block contenido %}
    <h1>Listado de alumnos</h1>
    <form action="{{ path('task_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <input type="submit" />
    </form>

{% endblock %}
